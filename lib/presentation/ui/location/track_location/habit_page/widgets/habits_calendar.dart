import 'package:flutter/material.dart';

class HabitsCalendar extends StatelessWidget {
  const HabitsCalendar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
